# v6 Live Readback Closeout

Date: 2026-06-25
Branch: `codex/v6-lifecycle-closeout`
Target org: Jurnii.io, `org20114906201`

Consolidated live Zoho readback across four clusters — workflow rules, fields, automation functions,
and email templates/notifications. This is a read-only capture: nothing in the live org was modified.
It records what was confirmed live and is explicit about what could NOT be verified via the MCP
connector.

## Live vs Repo — Material Findings

1. **Deluge function BODIES are not readable via MCP.** `getAutomationFunctions(functionId)` returns "Empty response received from API" for every id. Name/module/timestamp map 1:1 to repo `.deluge` files, but deployed body == branch main is UNPROVABLE through the connector. All 18 live functions show `modified_by=Timothy Solomon` with timestamps consistent with recent commits (handleCallOutcome 12:39, handleMeetingEvent 12:38 on 2026-06-25), but parity is inference only.
2. **Email-template COPY and ACTIVE/PUBLISHED status are not readable via MCP** — there is no Email Templates GET tool. `getEmailNotifications` returned exactly ONE object (a 'test' cadences action, id 991103000001866043, referencing template 991103000001488004 Onboarding-Agreement-Confirmed). All 31 lifecycle template subjects/bodies/merge-fields are doc-asserted only (TEMPLATE_REGISTRY/EMAIL_MANIFEST, created 2026-06-15), NOT live-verified today. Cannot confirm the lifecycle templates still exist live or carry corrected copy.
3. **ATTENDANCE-IMPLICATION risk on 'Demo Hosted - Initial 1' (991103000001476007):** the corrected EMAIL_MANIFEST copy implies attendance by design (trigger demo:followup, prior=demo held). But an older DRAFT in V5_EMAIL_ARCHITECTURE_REVIEW.md (lines 655-664) uses attendance-implying copy AND rejected merge syntax (`${Organization.Organization Name}`, no-bang tags). Cannot verify via MCP which copy the live template carries. With SEQ-1 routing missed demos INTO the Demo Hosted recovery cadence, the demo-hosted:1 opener will be sent to contacts who did NOT attend — the copy must not assume attendance for the recovery path.
4. **WF004 Commercials Status Handler (id 991103000000800001) is still ACTIVE** on Deals (field_update on Commercials_Status), per live readback — its own description marks it a cutover bridge to disable after WF021 publish + signature/atomic-Quote-write proofs + harness gates. Repo memory (v6-activity-state-migration) recorded full Commercials_Status retirement "approved 2026-06-20", but live state shows it un-retired. Divergence between approved-intent and deployed-state.
5. **handleEmailEvent (function 991103000000780331) is the ONLY live function with `associated=FALSE`** — present but not wired to any active automation. The five granular email handlers (Replied/OpenedNotReplied/NotReplied/Clicked/Bounced) are the live wiring. Confirm whether `v6/activity/handleEmailEvent.deluge` is still intended to be live or is superseded.
6. **WF006 Condition 2** (`Next_Follow_Up_Date != EMPTY AND Sequence_Managed=Yes`, id 991103000001977002) is CONFIRMED present live (modified 2026-06-25T11:56) — the "known live defect" (handler not invoked on a Next_Follow_Up_Date-only change) is already fixed at the workflow level and the idempotency retest PASSED. The audit brief still listing it as "known live" is stale. Because WF006 trigger is 'anyaction', a single save that populates both Call_Task_State and Next_Follow_Up_Date invokes handleCallOutcome TWICE — idempotency relies on the function, which cannot be body-verified via MCP.
7. **WF008 (Tasks, id 991103000000784145) has a SINGLE condition with criteria=null** and one handleTaskCompletion action + assign_owner — NO duplicate native Status=Completed branch (the previously-noted duplicate condition 991103000000797003 is deleted live). The only rule referencing native Tasks.Status is WFC-SchedEmail, and it gates on Status='Not Started', not 'Completed'. Live ruleset confirms NO active *_Outcome trigger on any module. TASK-3 routing concern is therefore CLOSED at the workflow level.
8. **ACTIVITY_ROUTING_MATRIX.md** (docs/v5, machine-generated by scripts/gen_activity_matrix.py) is STALE vs the 31-email canonical model: still emits dead keys onboarding:0:kickoff, demo-hosted:0:post-demo (vs canonical proposal-preparation:0:post-demo) and commercial-agreement:0:follow-up (removed as non-email). Needs regeneration from the current resolver config.
9. **spec.md internal inconsistencies** vs canonical docs: it names the first stage 'Marketing Qualification' (canonical/live = 'Marketing Consent') and names the Deal lifecycle fields bare 'State'/'Status' (canonical/live = Opportunity_State/Opportunity_Status). spec.md's Stage->Opportunity map also omits Demo Hosted and Proposal Preparation rows. Not reconciled.
10. **WF001c Process Account inner action** was read from list-summary only (not re-fetched via getWorkflowRuleById); modules outside the six (Leads/Quotes/Products/etc.) were not enumerated; `getAutomationFunctionFailures` count=0 but the retention window is undocumented so it is not proof of zero historical Meeting/Call/Task failures.

## Cluster 1 — Workflow Rules (11 active across 6 modules)

| Rule | Module | ID | Trigger | Function (action id) | Live criteria |
| --- | --- | --- | --- | --- | --- |
| WF006 Handle Call Outcome | Calls | 991103000000808046 | anyaction (create/edit, any action) | handleCallOutcome (991103000000780459, `callId=${Calls.id}`) | Cond1 991103000000808047: `Call_Task_State != EMPTY AND Sequence_Managed=Yes`; Cond2 991103000001977002 (added today): `Next_Follow_Up_Date != EMPTY AND Sequence_Managed=Yes` |
| WF008 Task Completion Handler | Tasks | 991103000000784145 | create_or_edit, repeat=false | handleTaskCompletion (991103000000780448, `taskId=${Tasks.id}`) + assign_owner `${!Tasks.Owner}` | Single condition 991103000000784146, criteria=null (runs on every create/edit). Duplicate Status=Completed condition 991103000000797003 DELETED live |
| WF007 Event Meeting Handler | Events | 991103000000782052 | create_or_edit, repeat=false | handleMeetingEvent (991103000000780415, `eventId=${Events.id}`) + assign_owner `${!Events.Owner}` | Single condition 991103000000782053, NO criteria (runs on all Event create/edit) |
| WF004 Commercials Status Handler | Deals | 991103000000800001 | field_update on Commercials_Status (991103000000783091), match_all=true | handleCommercialsStatusChange (991103000000780404) | Single condition 991103000000800002. ACTIVE legacy cutover bridge; last_executed 2026-06-16T16:05 |
| WF001b0 Process Contact | Contacts | 991103000000663630 | field_update, repeat=true | processContact (991103000000774779) | OR group: change to Stage(663408), State(663424), Status(663329), Contact_Role1(778281), Account_Name(457) |
| WF001b2 Process Contact | Contacts | 991103000001499202 | create | processContact (991103000000774779) | Single condition 991103000001499203, no criteria. Create-time companion to WF001b0 |
| WF001d Process Deal | Deals | 991103000000663638 | create_or_edit, repeat=true | processDeal (991103000000774797) | Single condition 991103000000663639, no criteria |
| WF001c Process Account | Accounts | 991103000000663648 | create_or_edit, repeat=true | processAccount (per convention; inner action not re-fetched) | No criteria noted at summary level |
| WF010c Date Router Demo Reminder | Deals | 991103000000802001 | date_or_datetime on Demo_Reminder_Send_At (991103000000784044), 15 min before, once | sendDemoReminder (991103000001499062, `dealId=${Deals.id}`) | Guard `Automation_Suppressed != true`. last_executed=null (never fired) |
| WF010d Date Router Comm Follow-Up | Deals | 991103000000790038 | date_or_datetime on Next_Comm_Follow_Up_Date (991103000000784052), 15 min, once | sendCommercialFollowUp (991103000001499072) | last_executed=null (never fired) |
| WFC-SchedEmail | Tasks | 991103000001499121 | date_or_datetime on Due_Date, 0 days, 09:00 +01:00, once | sendScheduledEmailFromTask (991103000001499119) | AND: `Task_Sequence_Managed=true AND native Status='Not Started' AND Task_Type='Scheduled Send'`. last_executed=null. ONLY rule gating on native Tasks.Status — gates on 'Not Started', not 'Completed' |

**Live workflow ids of interest:** WF004 991103000000800001, WF006 991103000000808046, WF007 991103000000782052, WF008 991103000000784145.

**Confirmed live findings:**

- WF006 fires on a `Next_Follow_Up_Date`-only change (Condition 2), provided `Sequence_Managed=Yes`. The 'anyaction' trigger double-invokes handleCallOutcome if both Call_Task_State and Next_Follow_Up_Date are saved together — idempotency must hold in the function.
- WF008 has no duplicate native `Status=Completed` branch; native/lifecycle status reconciliation is delegated entirely to the Deluge function.
- WF007 invokes handleMeetingEvent on all Event create/edit with no criteria (relevant to MTG-4 — there is no trigger-level guard against re-running terminal progression).
- NO active workflow rule triggers on any `*_Outcome` field across the six modules. Task_Outcome is explicitly NOT a lifecycle command.
- One legacy bridge remains ACTIVE: WF004 (handleCommercialsStatusChange) on Deals.Commercials_Status — the only surviving legacy `*_Status` trigger.
- Total active rules: Calls 1, Tasks 2, Events 1, Contacts 2, Deals 4, Accounts 1 = 11.
- Recent activity (2026-06-25): WF006 11:52, WF007 12:12, WF008 12:04, WF001b2/WF001c 12:33, WF001d 12:12. WF010c, WF010d, WFC-SchedEmail never fired.

**Could NOT verify live (workflow cluster):**

- WF001c Process Account inner action was reported from list-view summary + naming convention, not separately re-fetched via getWorkflowRuleById.
- Modules outside the six (Leads, Quotes, Products, Vendors, Cases, custom) were not enumerated.
- Function bodies for the bound handlers were not inspected (binding name+id only) — handleCallOutcome idempotency against the WF006 double-invoke must be verified against source, not rule metadata.
- No email-notification action objects were present on the inspected rules; standalone non-rule notification templates were not enumerated beyond the single 'test' action.

## Cluster 2 — Fields / Obsolete-Field Audit

See `OBSOLETE_FIELD_AUDIT.md` for the full per-field table. Live summary:

- Of the 8 named obsolete candidates: **Task_Outcome** (Tasks, active label, holds data) and the DEP-prefixed **Commercial_Outcome** + **Commercials_Status** (Deals) are LIVE; **Call_Outcome, Meeting_Outcome, Meeting_Status, Demo_Outcome, Demo_Status** are ALREADY ABSENT in all six modules.
- Task_Outcome is the only candidate still holding DATA (COQL: >=1 record, value 'Completed', id 991103000001651001) — highest-risk, blocked from delete.
- Commercial_Outcome and Commercials_Status are COQL-EMPTY (0 records). Commercial_Outcome is safe-to-delete after dependency check; Commercials_Status is blocked until WF004 is deactivated.
- State/Status/Stage/Lost_Reasons picklists are consistent across modules; the final Stage value is misspelled 'Renewall' (double-L) in Contacts.Stage, Task_Stage, Call_Task_Stage, Meeting_Task_Stage, Deals.Opportunity_Stage; Calls.Sequence_Stage and Events.Follow_Up_Stage spell 'Renewal' correctly.

**Could NOT verify live (field cluster):**

- COQL `count(id)` aggregate failed (SYNTAX_ERROR 'missing clause: group by'); exact historical row counts not obtainable — presence/absence probed instead.
- Dependency analysis (which Deluge functions/workflow rules reference Task_Outcome, Commercial_Outcome, Commercials_Status) was NOT performed — any field delete must be gated on a separate dependency check.
- Native State/Status value sets on Calls/Events not enumerable from saved metadata (system/non-editable). No fields were created, modified, hidden, renamed, or deleted.

## Cluster 3 — Automation Functions (18 live; repo mapping)

| Function | Function id / assoc id | Module | Repo file | Modified live | associated |
| --- | --- | --- | --- | --- | --- |
| processContact | 991103000000774692 / 991103000000774779 | Contacts | v6/processContact.deluge | 2026-06-25T08:24:54 | true |
| processDeal | 991103000000774697 / 991103000000774797 | Deals | v6/processDeal.deluge | 2026-06-24T22:22:22 | true |
| processLead | 991103000000774702 / 991103000000774791 | Leads | v6/processLead.deluge | 2026-06-24T22:22:12 | true |
| processAccount | 991103000000774687 / 991103000000774785 | Accounts | v6/processAccount.deluge | 2026-06-24T18:08:57 | true |
| handleMeetingEvent | 991103000000780334 / 991103000000780415 | Events | v6/activity/handleMeetingEvent.deluge | 2026-06-25T12:38:52 | true |
| handleCallOutcome | 991103000000780322 / 991103000000780459 | Calls | v6/activity/handleCallOutcome.deluge | 2026-06-25T12:39:04 (most recent) | true |
| handleTaskCompletion | 991103000000780337 / 991103000000780448 | Tasks | v6/activity/handleTaskCompletion.deluge | 2026-06-25T08:24:32 | true |
| handleCommercialsStatusChange | 991103000000780325 / 991103000000780404 | Deals | v6/activity/handleCommercialsStatusChange.deluge | 2026-06-22T09:46:13 | true |
| sendCommercialFollowUp | 991103000001499021 / 991103000001499072 | Deals | v6/activity/sendCommercialFollowUp.deluge | 2026-06-21T20:31:47 | true |
| **handleEmailEvent** | 991103000000780331 / 991103000000780517 | Deals (related Emails) | v6/activity/handleEmailEvent.deluge | 2026-06-21T20:00:42 | **FALSE** |
| handleQuoteStageChange | 991103000001581207 / 991103000001581241 | Quotes | v6/activity/handleQuoteStageChange.deluge | 2026-06-21T19:59:36 | true |
| sendScheduledEmailFromTask | 991103000001499011 / 991103000001499119 | Tasks | v6/activity/sendScheduledEmailFromTask.deluge | 2026-06-20T15:43:24 | true |
| sendDemoReminder | 991103000001499016 / 991103000001499062 | Deals | v6/activity/sendDemoReminder.deluge | 2026-06-20T15:43:15 | true |
| handleEmailReplied | 991103000000780542 / 991103000000780547 | Deals (related Emails) | v6/activity/handleEmailReplied.deluge | 2026-06-20T15:41:51 | true |
| handleEmailOpenedNotReplied | 991103000000780537 / 991103000000780568 | Deals (related Emails) | v6/activity/handleEmailOpenedNotReplied.deluge | 2026-06-20T15:41:40 | true |
| handleEmailNotReplied | 991103000000780532 / 991103000000780561 | Deals (related Emails) | v6/activity/handleEmailNotReplied.deluge | 2026-06-20T15:41:28 | true |
| handleEmailClicked | 991103000000780527 / 991103000000780574 | Deals (related Emails) | v6/activity/handleEmailClicked.deluge | 2026-06-20T15:41:06 | true |
| handleEmailBounced | 991103000000780522 / 991103000000780554 | Deals (related Emails) | v6/activity/handleEmailBounced.deluge | 2026-06-20T15:40:55 | true |

**Confirmed live findings:**

- 18 live functions, all `language=deluge`, `source=crm`, created/modified by Timothy Solomon; all four core processors + activity handlers map 1:1 by name to v6 repo files.
- **handleEmailEvent (991103000000780331) is `associated=FALSE`** — present but unwired. The five granular email handlers are the live email wiring; handleEmailEvent appears to be a superseded/standalone dispatcher kept but unbound. Confirm intent.
- `getAutomationFunctionFailures` returned ZERO failures (count=0) — for any function. (Retention window undocumented; not proof of zero historical errors.)
- Standalone/invoked sub-functions (routeContactSequence, sendSequencedEmail, createAuxTask, applyCommercialTransition, all `_util_*` helpers) do NOT surface in getAllAutomationFunctions (they are invoked via invokeFunction, not workflow-bound). Repo grep confirms they are referenced from the live-mapped handlers.
- ORPHANED files (_util_resolveQuotePlanSummary.ORPHANED, ensureDealQuote.ORPHANED, syncConfirmedQuoteToDeal.ORPHANED) correctly have no live counterpart.

**Could NOT verify live (function cluster):**

- **FUNCTION BODY NOT EXPOSED:** `getAutomationFunctions(functionId)` returns "Empty response received from API" for every id tried. The MCP exposes METADATA only (name, id, module, related_module, timestamps, associated/editable/deletable). Deployed Deluge body == repo `.deluge` is NOT confirmable — name/module/timestamp is the only correspondence.
- Round-trip body verification (read deployed -> diff repo) is impossible via this MCP; redeploys are write-without-readback.
- Live ids/up-to-dateness of standalone/invoked sub-functions cannot be confirmed (not enumerable via getAllAutomationFunctions).
- count=0 failures cannot be taken as a guarantee of no historical Meeting/Call/Task failures (retention window/scope undocumented).

## Cluster 4 — Email Templates / Notifications

8 canonical folders and 31 lifecycle templates are **doc-asserted** (TEMPLATE_REGISTRY.md +
EMAIL_MANIFEST.md, created 2026-06-15). Live readback could verify almost none of this.

**Folders (doc-asserted ids):** Marketing Consent 991103000001471001, Demo Booking 991103000001472001,
Demo Confirmation 991103000001471003, Demo Hosted 991103000001473001, Proposal Preparation
991103000001474001, Commercial Agreement 991103000001475001, Onboarding 991103000001472003, Renewal
991103000001476001.

**Only live-confirmed object:** Email Notification Action 'test' (id 991103000001866043, feature_type
cadences, module Contacts 991103000000000047) references template **991103000001488004 (Onboarding -
Agreement Confirmed)**, recipient_count 1, from/reply-to = user 991103000000543001 (Timothy Solomon),
created/modified 2026-06-25. This is the ONLY object `getEmailNotifications` returned and the only
template ID confirmed live today.

**Attendance-implication flag:** 'Demo Hosted - Initial 1' (991103000001476007, key
demo-hosted:1:initial) — the canonical EMAIL_MANIFEST copy implies attendance by design (trigger
demo:followup, prior=demo held). An older DRAFT in V5_EMAIL_ARCHITECTURE_REVIEW.md (lines 655-664) uses
attendance-implying copy AND rejected merge syntax. Under SEQ-1 the recovery cadence sends this opener
to non-attendees — the live copy must be confirmed not to assume attendance. Tracked in
`V6_EMAIL_COPY_VERIFICATION.md`.

**Could NOT verify live (template cluster):**

- TRUE email-template CRUD is NOT reachable via this MCP. The only template-aware tool is the workflow
  Email Notifications API, which exposes notification ACTIONS referencing a template by `{name,id}` only —
  no subject, body, active/published status, merge fields, or folder. There is no Email Templates GET tool
  and no readable email-template MCP resource.
- `getEmailNotifications` (per_page 200, all feature_types) returned exactly ONE record (the 'test'
  cadences action). None of the 31 lifecycle templates' subjects/bodies/active-status could be read live.
  Cannot confirm the lifecycle templates still exist live, nor their copy, nor whether 'Demo Hosted -
  Initial 1' carries corrected vs attendance-implying draft copy.
- Folder existence/names could not be live-verified (ids documented only).
- Merge-field structure inside each template could not be read live (reported tags are the empirically
  verified `${!<Module API>.<Field API>}` set from EMAIL_MANIFEST, verified 2026-06-15, not re-verified today).
- The lifecycle workflow email-notification actions binding templates to Contacts rules were NOT found
  live — either the lifecycle emails are sent via Deluge `sendSequencedEmail` (by template ID, consistent
  with TEMPLATE_REGISTRY's "send by ID" design) or the notification actions were never created. Recipient/
  active-binding readback for the 31 lifecycle templates is unavailable via MCP.
- True template copy/active-status would require an Email Templates API or a UI/Deluge invokeConnector
  readback — neither is exposed here.
