# docs/archive — historical references (pre-v6)

These documents are retained for history only. They predate the current **v6 contact-centric** model
and are **not authoritative** — do not use them to configure live automation.

**Current authority:** `docs/v6/` — `SINGLE_FIELD_AUTOMATION_AUDIT.md`, `E2E_TEST_HARNESS.md`,
`SINGLE_FIELD_E2E_TEST_PLAN.md`, `FLOW_REFERENCE.md`, `full-flow.mermaid`, `single-field-full-flow.mermaid`.
**v5 cutover record:** `docs/v5/` (with `docs/v5/archive/` for superseded v5 operational checklists).

Archived 2026-06-20 during the v6 hygiene sprint. Files here either carried a `SUPERSEDED` marker, were
v4/pre-v4 exploratory designs (`v1/`, `v2/`), were unreferenced legacy field/UI docs, or were tombstone
redirects. Git history preserves their original locations.
